# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/

# Route data — only the bundled example stages (examples/) are tracked;
# keep your own routes and manifests in local/ (ignored).
*.gpx
!examples/*.gpx
local/
misc/
*-backup.json

# Editor / OS
.DS_Store
.claude/
.idea/
.vscode/

# Claude artifact downloads
*.dc.html
